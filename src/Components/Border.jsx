import React from 'react';

const Border = ({className}) => {
    return (
        <div className={`border-b border-b-gray-300 ${className}`}>
            
        </div>
    );
};

export default Border;